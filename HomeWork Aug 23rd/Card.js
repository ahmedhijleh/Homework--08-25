

// Creates a question,answer for each card
var card = function(front,back) {
  var  q1= new card("who was the first president of the United States?","George Washington");

      if (err) {
        console.log(err);
      }
      console.log(JSON.stringify(result, null, 2));
   

module.exports = card;
